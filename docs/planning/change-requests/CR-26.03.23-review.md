# Review: [CR-26.03.23](CR-26.03.23.md)

> **Status**: Stage 2 review complete
> **Change request**:
> [docs/planning/change-requests/CR-26.03.23.md](CR-26.03.23.md)
> **Reviewer references**:
> [docs/policies/common/planning-model.md](../../policies/common/planning-model.md),
> [docs/implementation-plan.md](../../implementation-plan.md),
> [docs/execution/M4-1-review.md](../../execution/M4-1-review.md),
> [docs/execution/M4-2-review.md](../../execution/M4-2-review.md),
> [docs/design/linear-domain-coverage-audit.md](../../design/linear-domain-coverage-audit.md),
> [README.md](../../../README.md),
> [src/context_sync/version.py](../../../src/context_sync/version.py),
> [docs/future-work.md](../../future-work.md),
> [docs/policies/common/release-workflow.md](../../policies/common/release-workflow.md),
> [docs/policies/common/documentation-workflow.md](../../policies/common/documentation-workflow.md)

## Findings

| ID | Severity | Area | Finding | Evidence | Impact | Recommendation |
| --- | --- | --- | --- | --- | --- | --- |
| CR-R1 | Medium | Test Strategy | The CR correctly identifies the fake-gateway-only runtime as the current dead end and proposes M5 to close that gap. However, neither M5-1 nor M5-2 explicitly distinguishes which tests must exercise the real Linear API from which tests may use the existing fake gateway through the real adapter interface. M5-1's test column lists "Adapter contract tests for all gateway operations, gateway-backed integration tests for `sync` / `refresh` / `add` / `remove-root` / `diff`" without specifying whether "gateway-backed" means real Linear or fake-backed through the adapter contract. M5-2's test column lists "install-and-run smoke validation against a maintained gateway-backed environment" without stating whether that environment is a real Linear workspace or a comprehensive test fixture. The M5 exit criteria say "supported integration validation for the real public entry points" without distinguishing automated fake-backed integration tests from real-environment validation. | [CR-26.03.23.md:104](CR-26.03.23.md#L104) (M5-1 tests), [CR-26.03.23.md:105](CR-26.03.23.md#L105) (M5-2 tests), [CR-26.03.23.md:151–157](CR-26.03.23.md#L151) (M5 exit criteria), [docs/execution/M4-2-review.md:22](../../execution/M4-2-review.md#L22) (M4-2-R1: the fundamental gap is that nothing runs against real Linear) | If both M5-1 and M5-2 automated tests run only against the fake gateway adapter, the "real gateway" is never exercised by automated tests — only by the manual smoke recipe. That is a large improvement over the status quo but still leaves real-environment correctness dependent on a single manual pass. A query-format error, missing field, or auth-flow issue in the real adapter would not be caught until that manual step. | Clarify the test boundary in M5-1's detailed notes: state whether its "gateway-backed integration tests" are expected to exercise the real Linear API or the fake adapter, and if real-environment automated testing is deferred entirely to M5-2's manual smoke path, note that as an explicit test-strategy decision. Sharpen M5 exit criterion 2 to distinguish the two test levels (automated fake-backed integration tests vs. real-environment smoke validation). |
| CR-R2 | Low | Sequencing | M5-2 validates the public CLI and library surface but does not depend on M4-R1 or M4-R2. M4-R1 explicitly considers whether the command surface should be simplified (sync/add overlap, remove-root factoring) and M4-R2 may accept API changes beyond M4-3. If those reviews produce follow-on surface changes, M5-2's CLI integration test suite and smoke recipe would need updating. The impact is limited because M5-2's primary purpose is gateway validation (proving the real adapter works through the public entry points), not command-surface validation, and M6-1 (which does depend on M4-R2 and M4-3) provides the final docs alignment pass. | [CR-26.03.23.md:105](CR-26.03.23.md#L105) (M5-2 depends on M5-1 and M4-2 but not M4-R1/M4-R2), [docs/implementation-plan.md:471](../../implementation-plan.md#L471) (M4-R1 scope includes command-surface evaluation), [docs/implementation-plan.md:472](../../implementation-plan.md#L472) (M4-R2 may produce follow-on API changes), [CR-26.03.23.md:209](CR-26.03.23.md#L209) (M6-1 already gates on M4-R2 and M4-3) | M5-2 test artifacts could validate a pre-review CLI/API surface that changes shortly after. Not a correctness risk for the 1.0.0 boundary (M6-R1 evaluates the final shipped state), but it could leave stale test coverage in the repository. | Add a note in M5-2's detailed notes that its CLI/library test suite must be refreshed if M4-R1/M4-R2 accept follow-on surface changes, so the test artifacts stay aligned with the shipped surface through the 1.0.0 boundary. |

## Reviewer Notes

### Scope Fit

The CR correctly identifies the three gaps between the current M4 boundary and a
credible 1.0.0: the missing real gateway, the missing supported docs surface,
and the missing release process. Each gap is grounded in specific review findings
(M4-1-R1, M4-2-R1, M4-2-R2, M4-2-R3), the README's pre-release notice, and
the relevant common-policy documents. The two-milestone split — gateway first,
then docs/release — respects the natural sequencing: you cannot document or
release a runtime that does not work.

### Candidate Decisions

All seven candidate decisions are well-reasoned. In particular:

- Dropping the "one broad post-M4 ticket" option in favor of a two-milestone
  split correctly recognizes that the missing scope spans independent
  deliverables with different sequencing and review needs.
- Dropping the "keep release workflow out-of-band" option correctly recognizes
  that the release process involves material product-delivery requirements
  (checklist, version-state guardrails, next-cycle bootstrap) that should be
  planned, not improvised.
- Dropping the "defer real gateway to future work" option correctly recognizes
  that a fake-gateway-only 1.0.0 is not credible.

### Sequencing and Dependencies

The dependency graph is sound:

- M5-1 depends on M1-D2 (adapter boundary), M3-O1 (comments_signature
  settlement), and M4-1 (CLI surface) — all Done. M5-1 can start immediately.
- M5-2 depends on M5-1 and M4-2 — correct, since public validation requires
  both the real gateway and the logging/validation infrastructure.
- M6-1 depends on M5-2, M4-R2, and M4-3 — correct, since docs must reflect
  the validated real runtime and the settled API surface. The transitive chain
  through M4-R2 to M4-R1 ensures the docs also wait for the CLI review
  outcome.
- M6-2 depends on M6-1 — correct, since the release process must validate the
  docs surface.
- M6-R1 depends on M6-1 and M6-2 — correct, since the readiness review
  evaluates the total shipped boundary.
- M6-3 depends on M6-O1 and M6-R1 — correct, since the release cut needs both
  publication prerequisites and readiness confirmation.

The remaining M4 tickets (M4-R1, M4-R2, M4-3) are correctly left unchanged,
and their outcomes flow into M6-1 through the existing dependency chain. The
CR's M6-1 detailed notes explicitly handle the case where M4-R2 produces
additional follow-on API changes beyond M4-3.

### Done-Ticket Amendment Rule

The CR correctly represents all new work as new follow-on tickets rather than
widening completed tickets. M5-1 cites M1-D2, M3-O1, and M4-1 as satisfied
prerequisites — appropriate since those tickets' accepted repository artifacts
already support the new dependency. M5-2 is the explicit defer destination for
M4-2-R3, correctly making that deferral relationship traceable.

### Missing Prerequisites

No missing prerequisites identified. M1-O1 (live Linear validation
environment) is already Done and implicitly available for M5 gateway
verification. M6-O1 correctly covers the release publication prerequisites
that are a human decision.

### Missing Design Items

No missing design tickets. The gateway adapter boundary is fully defined by
M1-D2. The release workflow framework exists in common policy. The docs
surface does not need a separate design document.

### Over- or Under-Commitment

The scope is proportional. Seven new named items (two milestones) to close
three identified gaps is appropriate. The individual ticket scopes are
well-bounded: M5-1 is constrained by the M1-D2 boundary, M5-2 is constrained
by the existing public surface, M6-1 is constrained by the shipped behavior,
M6-2 is constrained by the common release-workflow policy, and M6-3 is the
mechanical execution of M6-2's defined process.

### Backlog-Source Traceability

Good. Every proposed item traces to specific review findings (M4-1-R1,
M4-2-R1, M4-2-R2, M4-2-R3), the README pre-release notice, the active plan,
or the common policy documents. The CR's candidate-sources section is complete.

## Residual Risks

- The CR does not address whether `linear-client` v1.0.0 will prove sufficient
  for all M5-1 gateway operations. The M5-1 detailed notes acknowledge this
  risk ("If `linear-client` 1.0.0 proves insufficient...") but the plan-level
  risk register update in section 5 should also carry this risk explicitly,
  since a `linear-client` gap discovered during M5-1 could block the entire
  1.0.0 path.
- The M5-2 manual smoke recipe is the sole real-environment validation gate
  for the 1.0.0 release. If that recipe is not thorough enough, production
  users will be the first to exercise certain gateway paths against real
  Linear.
