# CR-26.03.24-policy-compliance - Common-Policy Compliance Gap Analysis

> **Status**: Approved — activated 2026-03-24
> **Target active plan**:
> [docs/implementation-plan.md](../../implementation-plan.md)
> **Governing process**:
> [docs/policies/common/planning-model.md](../../policies/common/planning-model.md)

## Candidate Sources Consulted

- [docs/policies/common/coding-guidelines.md](../../policies/common/coding-guidelines.md) -
  `scripts/validate.sh` requirement (quality gate entry point)
- [docs/policies/common/python/coding-guidelines.md](../../policies/common/python/coding-guidelines.md) -
  Pyright requirement, `py.typed` marker, PEP 561 packaging, semantic types and
  constrained string aliases, interface documentation dimensions
- [docs/policies/common/coding-guidelines.md §All interfaces must be fully documented](../../policies/common/coding-guidelines.md) -
  ten required documentation dimensions for public interfaces
- [docs/policies/common/python/agent-awareness.md](../../policies/common/python/agent-awareness.md) -
  `coverage.py` tooling, `examples/` directory, `docs/design/dependency-map.md`,
  maturity composite score dimensions
- [docs/policies/common/data-modeling.md](../../policies/common/data-modeling.md) -
  Pydantic vs `dataclass` decision rule
- [docs/policies/common/reviews/code-review.md](../../policies/common/reviews/code-review.md) -
  cross-role review checklist (new artifact)
- [docs/policies/common/reviews/design-review.md](../../policies/common/reviews/design-review.md) -
  cross-role design review checklist (new artifact)
- [docs/policies/common/implementation-plan-template.md](../../policies/common/implementation-plan-template.md) -
  reusable plan layout (new template)
- [docs/policies/common/release-checklist-template.md](../../policies/common/release-checklist-template.md) -
  reusable release checklist layout (new template)
- [docs/policies/common/README.md](../../policies/common/README.md) -
  client repository bootstrap expectations
- [docs/implementation-plan.md](../../implementation-plan.md) -
  active plan (M5/M6 scope)
- Current repository state as of 2026-03-24

## Gap Summary

The common-policy layer has added or materially updated several modules since the
active plan was last amended. This analysis classifies each gap as either:

- **Immediate** - not covered by any existing plan item and should be addressed
  before M5 work begins, because later milestones will build on the corrected
  baseline
- **Plan-covered** - already addressed by an existing M5 or M6 ticket
- **Advisory** - new policy artifact that does not create a compliance gap but
  changes how existing work is evaluated

### Immediate gaps (not covered by active plan)

| # | Gap | Policy source | Current state | Impact |
| --- | --- | --- | --- | --- |
| G-1 | `scripts/validate.sh` missing | [coding-guidelines.md](../../policies/common/coding-guidelines.md) | No single quality gate entry point; `ruff check` and `pytest` run ad hoc | All non-docs-only tickets require "declared linting and formatting commands pass"; without `validate.sh` there is no canonical command to satisfy that gate |
| G-2 | Pyright not configured | [python/coding-guidelines.md](../../policies/common/python/coding-guidelines.md) | Ruff is the only static analysis tool; Pyright is not in dev deps and has no `pyproject.toml` config | Policy requires "Pyright required as static type checker (in addition to Ruff)" with all errors resolved before completion |
| G-3 | `py.typed` marker missing | [python/coding-guidelines.md](../../policies/common/python/coding-guidelines.md) | No `src/context_sync/py.typed` file; no `[tool.setuptools.package-data]` entry | PEP 561 inline type annotations are not discoverable by downstream consumers or type checkers |
| G-4 | `coverage.py` / `pytest-cov` not configured | [python/agent-awareness.md](../../policies/common/python/agent-awareness.md) | Neither `coverage[toml]` nor `pytest-cov` in dev deps; no `[tool.coverage.*]` config | Branch coverage is not measured; the maturity composite score cannot be computed |
| G-5 | `examples/` directory missing | [python/agent-awareness.md](../../policies/common/python/agent-awareness.md) | No `examples/` directory at repository root | Policy requires "one runnable script per major documented workflow" |
| G-6 | `docs/design/dependency-map.md` missing | [python/agent-awareness.md](../../policies/common/python/agent-awareness.md) | Design docs exist but no dependency map | Policy requires main entry points, internal module summaries, workflow-to-code map, anti-patterns, and async ownership |
| G-7 | README common-policy bootstrap section incomplete | [common README.md](../../policies/common/README.md) | README already covers common-policy dependency, symlink setup, recovery, and common-layer reference. Three targeted gaps remain: `ripgrep` absent from system prerequisites, `cloc` listed only in Developer commands rather than in Prerequisites, and no explicit symlink verification step | New contributors following the Prerequisites section alone will miss two required system tools and have no way to confirm the symlink is working |
| G-8 | No semantic type differentiation | [python/coding-guidelines.md §Semantic Types](../../policies/common/python/coding-guidelines.md) | Zero `NewType` or `Annotated[str, StringConstraints(...)]` usages in the source tree. At least seven distinct domain concepts (`IssueId`, `IssueKey`, `WorkspaceId`, `WorkspaceSlug`, `CommentId`, `AttachmentId`, `WriterId`) are bare `str` across all major modules. No `_types.py` module exists. | Once Pyright lands, bare `str` passes unchecked where semantically distinct types should be distinguished. Blocks the maturity composite score's "semantic types used at public boundaries" inspectability item. |
| G-9 | Public interface docstrings missing required dimensions | [coding-guidelines.md §All interfaces must be fully documented](../../policies/common/coding-guidelines.md) | The four `ContextSync` public methods (`sync`, `refresh`, `remove`, `diff`) are missing realistic examples, idempotency statements, mutability statements, and thread-safety statements. `LinearGateway` protocol methods have incomplete parameter/output documentation and missing `Raises` sections. | Ten documentation dimensions are a hard policy requirement. The missing idempotency and mutability dimensions are particularly relevant because `sync`, `refresh`, and `remove` mutate the context directory with non-obvious repeat-call behavior. |

### Gaps covered by active plan (no amendment needed)

| Gap | Policy source | Covered by |
| --- | --- | --- |
| Release scripts (`build_release.sh`, `build_validate.sh`, `next_release.sh`, `check_version_state.py`) | [python/release-workflow.md](../../policies/common/python/release-workflow.md) | [M6-2](../../implementation-plan.md#m6-2---canonical-release-workflow-checklist-and-version-state-guardrails) |
| `CHANGELOG.md` | [coding-guidelines.md](../../policies/common/coding-guidelines.md) | [M6-2](../../implementation-plan.md#m6-2---canonical-release-workflow-checklist-and-version-state-guardrails) |
| MkDocs / supported docs surface | [python/documentation-workflow.md](../../policies/common/python/documentation-workflow.md) | [M6-1](../../implementation-plan.md#m6-1---supported-user-facing-docs-and-installoperator-guide) |
| Release checklist artifact | [release-workflow.md](../../policies/common/release-workflow.md) | [M6-2](../../implementation-plan.md#m6-2---canonical-release-workflow-checklist-and-version-state-guardrails) |
| `pyright --verifytypes` pre-release gate | [python/coding-guidelines.md](../../policies/common/python/coding-guidelines.md) | [M6-R1](../../implementation-plan.md#m6-r1---100-release-readiness-review) (release-readiness review scope) |

### Advisory (new policy artifacts, no compliance gap)

| Artifact | Purpose | Effect on this repository |
| --- | --- | --- |
| [data-modeling.md](../../policies/common/data-modeling.md) | Pydantic vs `dataclass` decision rule | Repository already uses Pydantic for serialization-boundary models and has no plain-`dict` data models; compliant as-is |
| [reviews/code-review.md](../../policies/common/reviews/code-review.md) | Cross-role code review checklist | Available for future Phase B reviews; does not create a retroactive gap |
| [reviews/design-review.md](../../policies/common/reviews/design-review.md) | Cross-role design review checklist | Available for future Phase B reviews; does not create a retroactive gap |
| [implementation-plan-template.md](../../policies/common/implementation-plan-template.md) | Reusable plan layout | Active plan predates the template; no retroactive reformatting required |
| [release-checklist-template.md](../../policies/common/release-checklist-template.md) | Reusable release checklist layout | Available for M6-2 when the release checklist is authored |

## Proposed Amendment

If accepted, this amendment would add a new **Milestone 4.2** between
Milestone 4.1 and Milestone 5 with the scope limited to closing the nine
immediate compliance gaps identified above. This keeps M5 focused on the real
gateway and ensures the compliance baseline is in place before later milestones
build on it.

### Rationale for a separate milestone

- G-1 through G-4 and G-8 affect the quality gate, type-checking, and semantic
  type baseline that every subsequent implementation ticket will run against.
  Landing them inside M5 would mean the first M5 ticket must carry both gateway
  work and toolchain changes simultaneously. G-8 (semantic types) must land
  alongside Pyright (G-2) so the type checker validates the new `NewType`
  boundaries from the start rather than leaving bare `str` to pass unchecked.
- G-5, G-6, and G-9 are documentation/navigability artifacts that become more
  useful once the real gateway exists but whose structure should be established
  now while the public API surface is freshly settled after M4.1.
- G-7 is a targeted README gap that blocks new contributors from confirming
  system prerequisites and verifying the common-policy symlink.

### Candidate Decisions

| Candidate | Decision | Rationale |
| --- | --- | --- |
| Address all nine gaps as ad hoc work outside the plan | Drop | The gaps are material enough to warrant tracked execution artifacts; ad hoc work would leave no review trail |
| Fold the gaps into existing M5 tickets | Drop | M5 tickets are scoped to gateway and runtime validation; mixing toolchain/docs compliance work dilutes both scopes and makes Phase B review harder |
| Add the gaps as tickets inside Milestone 4.1 | Drop | M4.1 is already complete (both tickets at Phase C done); reopening it violates the done-ticket amendment rule |
| Create a new Milestone 4.2 for policy compliance | Keep | Clean milestone boundary, minimal disruption to M5/M6 scope, and all nine gaps get tracked execution artifacts with Phase B review |

### Proposed Milestone 4.2: Common-Policy Compliance

**Goal:** close the immediate common-policy compliance gaps so that M5 and later
milestones build on a conforming toolchain, type-checking, coverage, and
documentation baseline.

#### Milestone Overview Row

| Milestone | Name | Primary deliverable |
| --- | --- | --- |
| `M4.2` | Common-policy compliance | `scripts/validate.sh`, Pyright integration, semantic types (`_types.py`), `py.typed` marker, `coverage.py` tooling, `examples/` directory, `docs/design/dependency-map.md`, public interface docstring completion, and README common-policy bootstrap fixes |

#### Implementation Tickets

| # | Status | Ticket | Description | Dependencies | Tests | Source |
| --- | --- | --- | --- | --- | --- | --- |
| M4.2-1 | Todo | Quality gate entry point, static analysis baseline, and semantic types | Create `scripts/validate.sh` that runs `ruff check`, `ruff format --check`, `pyright`, and `pytest` in sequence, exiting non-zero on any failure. Add `pyright` to dev deps. Add `[tool.pyright]` config to `pyproject.toml`. Create `src/context_sync/py.typed` and add `[tool.setuptools.package-data]` entry. Create `src/context_sync/_types.py` with `NewType` aliases for the identified domain concepts (`IssueId`, `IssueKey`, `WorkspaceId`, `WorkspaceSlug`, `CommentId`, `AttachmentId`, `WriterId`; constrained-string or `NewType` aliases for RFC 3339 timestamps and similar). Adopt the new types across the source tree. Resolve all Pyright errors. | [M4.1-2](../../execution/M4.1-2.md) | `validate.sh` exits 0 on clean tree; `py.typed` is included in built wheel; Pyright reports zero errors; `_types.py` defines `NewType` aliases for all identified domain concepts; no bare `str` remains for semantically distinct domain fields at public boundaries | G-1, G-2, G-3, G-8 |
| M4.2-2 | Todo | Coverage tooling, agent-awareness artifacts, and interface documentation | Add `coverage[toml]>=7,<8` and `pytest-cov>=5,<6` to dev deps. Add `[tool.coverage.run]` and `[tool.coverage.report]` config. Update `scripts/validate.sh` to include `--cov --cov-report=term-missing` flags. Create `examples/` directory with at least one runnable script exercising `ContextSync` via the fake gateway. Create `docs/design/dependency-map.md` with entry points, module summaries, workflow-to-code map, anti-patterns, and async ownership. Complete the missing docstring dimensions on all four `ContextSync` public methods and `LinearGateway` protocol methods. Update README to add `ripgrep` and `cloc` to system prerequisites and add an explicit symlink verification step. | [M4.2-1](#m4.2-1) | `pytest --cov --cov-report=term-missing` produces branch coverage report; `validate.sh` includes coverage flags; example script runs without error; dependency map covers all public entry points; all `ContextSync` public methods document all ten required dimensions; `LinearGateway` protocol methods have complete parameter, output, and `Raises` documentation; README prerequisites list `ripgrep` and `cloc`; README includes symlink verification step | G-4, G-5, G-6, G-7, G-9 |

#### Detailed Ticket Notes

##### M4.2-1 - Quality gate entry point, static analysis baseline, and semantic types

- `scripts/validate.sh` is the canonical "validate everything" command per
  [coding-guidelines.md](../../policies/common/coding-guidelines.md). It must
  run all declared quality gates and exit non-zero on any failure.
- Pyright config should target the same Python version as the rest of the
  toolchain (3.13). Use `pythonVersion = "3.13"` and `typeCheckingMode = "standard"`
  as the starting baseline; tighten to `strict` only if zero errors is achievable
  without excessive `type: ignore` annotations.
- Resolving Pyright errors may require adding or tightening type annotations in
  some modules. This is expected and should be treated as part of the ticket scope.
- The `py.typed` marker is a zero-byte file at `src/context_sync/py.typed`. The
  `[tool.setuptools.package-data]` entry ensures it ships in the wheel.
- Create `src/context_sync/_types.py` as the dedicated module for reusable type
  aliases per
  [python/coding-guidelines.md §Semantic Types](../../policies/common/python/coding-guidelines.md).
  At minimum define `NewType` aliases for: `IssueId`, `IssueKey`, `WorkspaceId`,
  `WorkspaceSlug`, `CommentId`, `AttachmentId`, `WriterId`. For RFC 3339
  timestamps and other constrained strings, `Annotated[str, StringConstraints(...)]`
  or `NewType` aliases are both acceptable.
- Adopt the new types across the source tree, replacing bare `str` at public
  boundaries. The type migration touches every module that handles issue IDs,
  issue keys, workspace IDs, and similar domain concepts. This is a wide
  mechanical change — scope the initial `NewType` set to the minimum that closes
  the policy gap rather than attempting a maximal migration. If the migration
  surfaces Pyright errors that are actually logic bugs (e.g. passing an
  `IssueKey` where an `IssueId` is expected), treat those as valuable
  discoveries and fix them as part of this ticket.
- Introducing semantic types and the type checker in the same ticket ensures
  Pyright validates the new type boundaries from the start rather than leaving
  bare `str` to pass unchecked.
- Update `scripts/validate.sh` in lockstep with any future toolchain changes so
  the script remains the single source of truth for quality gates.

##### M4.2-2 - Coverage tooling, agent-awareness artifacts, and interface documentation

- Coverage config should enable branch coverage and source from `src/`, omitting
  `version.py` per
  [python/agent-awareness.md](../../policies/common/python/agent-awareness.md).
- Update `scripts/validate.sh` to invoke `pytest --cov --cov-report=term-missing`
  instead of bare `pytest`. This is the lockstep obligation from M4.2-1: when
  a new quality gate tool is added, the canonical script must be updated in the
  same ticket that introduces the tool.
- The `examples/` script must be runnable against the fake gateway since the real
  gateway is not yet available. Use the `_gateway_override` testing hook with a
  brief comment explaining that the example will switch to the real constructor
  once the gateway lands in M5.
- The dependency map should follow the minimum content requirements from
  [python/agent-awareness.md](../../policies/common/python/agent-awareness.md):
  main entry points, key internal modules, workflow-to-code map, anti-patterns,
  and async ownership. Keep to 1-2 pages. **Scope risk:** if the dependency map
  writing blocks the other deliverables, the ticket owner should land the
  independent deliverables (coverage tooling, `examples/`, README fixes) first
  rather than holding the entire ticket.
- Complete the missing docstring dimensions on all four `ContextSync` public
  methods (`sync`, `refresh`, `remove`, `diff`): add realistic examples,
  idempotency statements, mutability statements, and thread-safety statements.
  Complete `LinearGateway` protocol method documentation: add missing parameter
  type documentation and `Raises` sections across `get_workspace_identity`,
  `get_ticket_relations`, `get_refresh_issue_metadata`,
  `get_refresh_comment_metadata`, `get_refresh_relation_metadata`. Note that the
  `LinearGateway` protocol method documentation may need updating again when
  M5-1 implements the real gateway.
- The README already covers common-policy dependency, symlink setup, recovery,
  and common-layer reference. The three targeted additions are: (1) add `ripgrep`
  to the Prerequisites section, (2) promote `cloc` from Developer commands into
  the Prerequisites section, and (3) add an explicit symlink verification step.

#### Exit Criteria

1. `scripts/validate.sh` runs all quality gates (Ruff lint, Ruff format check,
   Pyright, pytest with coverage) and exits 0 on a clean tree.
2. Pyright reports zero errors across the source tree.
3. `py.typed` is present and included in the built wheel.
4. `_types.py` exists and defines `NewType` aliases for all identified domain
   concepts; no bare `str` remains for semantically distinct domain fields at
   public boundaries; Pyright validates the `NewType` boundaries with zero
   errors.
5. `pytest --cov --cov-report=term-missing` produces a branch coverage report.
6. `examples/` contains at least one runnable script that exercises the primary
   `ContextSync` workflow.
7. `docs/design/dependency-map.md` covers all minimum-content requirements.
8. All four `ContextSync` public methods document all ten required interface
   dimensions. `LinearGateway` protocol methods have complete parameter, output,
   and `Raises` documentation.
9. README Prerequisites section lists `ripgrep` and `cloc` as system
   prerequisites. README includes an explicit symlink verification step.

### Proposed Active-Plan Changes

#### 1. Add Milestone 4.2 to the Milestone Overview

Insert after the `M4.1` row:

| Milestone | Name | Primary deliverable |
| --- | --- | --- |
| `M4.2` | Common-policy compliance | `scripts/validate.sh`, Pyright integration, semantic types (`_types.py`), `py.typed` marker, `coverage.py` tooling, `examples/` directory, `docs/design/dependency-map.md`, public interface docstring completion, and README common-policy bootstrap fixes |

#### 2. Add Milestone 4.2 section

Insert a new section between Milestone 4.1 and Milestone 5 with the ticket
tables and detailed notes from this change request.

#### 3. Update M5-1 dependencies

Add `M4.2-1` as a dependency of `M5-1` so that the real gateway implementation
ticket runs against the full quality gate baseline including Pyright and
semantic types.

#### 4. Update M5-2 dependencies

Add `M4.2-2` as a dependency of `M5-2` so that the supported public runtime
validation milestone builds on the full toolchain baseline including coverage
reporting. Without this dependency, M5-2's validation work could proceed without
branch-coverage tooling in place, and the coverage gap would not be caught until
M6-R1.

#### 5. Update future-work.md

No future-work items are being promoted. The gaps addressed here are policy
compliance requirements, not deferred feature work.
