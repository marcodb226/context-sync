# Review: [CR-26.04.07](CR-26.04.07.md)

> **Status**: Stage 2 review complete (2 passes)
> **Reviewed change request**:
> [docs/planning/change-requests/CR-26.04.07.md](CR-26.04.07.md)
> **Target active plan**:
> [docs/implementation-plan.md](../../implementation-plan.md)
> **Governing process**:
> [docs/policies/common/planning-model.md](../../policies/common/planning-model.md)
> **Reviewer references**:
> [docs/policies/common/planning-model.md](../../policies/common/planning-model.md),
> [docs/implementation-plan.md](../../implementation-plan.md),
> [docs/execution/M3-O1.md](../../execution/M3-O1.md),
> [src/context_sync/_gateway.py](../../../src/context_sync/_gateway.py),
> [linear-client CHANGELOG.md](../../../../linear-client/CHANGELOG.md)

This review was performed as the independent Stage 2 review pass for the draft
change request.

## Findings

| ID | Severity | Status | Area | Finding | Evidence | Impact | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CR-26.04.07-R1 | Medium | Todo | Amendment completeness | The CR creates M5-D1 (design audit) and makes M5-1 depend on it, but does not specify what happens if M5-D1 identifies required changes beyond M5-1's scope. The CR's own capability tables list v1.1.0 features that affect code outside the gateway: semantic type aliases affect [`_types.py`](../../../src/context_sync/_types.py), `User.app` affects the renderer, `LinearNotFoundError` affects orchestration error handling. If M5-D1 surfaces changes needed in existing code, the `LinearGateway` protocol, or other artifacts, there is no stated mechanism to represent those as new follow-on tasks or scope amendments. | [CR-26.04.07.md §"Additional v1.1.0 Capabilities"](CR-26.04.07.md), [CR-26.04.07.md §"Proposed Active-Plan Changes" §2](CR-26.04.07.md) — M5-1 updates are the only implementation-side consequence defined | M5-D1 findings that reach beyond M5-1's scope have nowhere to land. They would either be silently absorbed into M5-1 (widening its scope without an amendment), ignored, or require an ad hoc follow-up amendment that the CR could have anticipated. | Add a clause to the M5-D1 detailed notes stating that if the audit identifies required changes to existing done-ticket code, the `LinearGateway` protocol, or artifacts outside M5-1's scope, those must be represented as new follow-on tasks or scope amendments to existing Todo tickets rather than silently absorbed into M5-1 or left untracked. |
| CR-26.04.07-R2 | Medium | Todo | Traceability | The CR lists v1.1.0 capabilities under "Additional v1.1.0 Capabilities Available to the Gateway" and "Remaining v1.1.0 Gaps" but does not map the predictable consequences to existing Todo tickets. Several capabilities overlap with work already scoped into existing tickets: semantic type aliases partially overlap with M4.2-1 (Done, established [`_types.py`](../../../src/context_sync/_types.py)), `User.app` and `Issue.get_comments()` root-thread projection affect the renderer built by M2-2 (Done) and wired by M5-1 (Todo). The CR should state which known v1.1.0 consequences are already covered by existing Todo tickets, which are deferred to M5-D1's findings, and which are mechanical fixes that can be tracked independently. Without this mapping, a reader cannot tell whether the CR's capability tables are informational context or untracked obligations. | [CR-26.04.07.md §"Additional v1.1.0 Capabilities"](CR-26.04.07.md), [docs/implementation-plan.md M4.2-1](../../implementation-plan.md), [docs/implementation-plan.md M5-1](../../implementation-plan.md) | A future session executing M5-1 might not realize it is also expected to handle renderer-side changes for `User.app` or root-thread comment traversal, since those are listed in a CR capability table rather than in M5-1's ticket notes. | Add a brief disposition column or paragraph to the capability tables mapping each listed item to its planned resolution: "covered by M5-1," "deferred to M5-D1 findings," "mechanical fix independent of the audit," or "informational only — no current v1 use." |
| CR-26.04.07-R3 | Low | Todo | Historical accuracy | The M3-O1 execution artifact ([docs/execution/M3-O1.md:145–146](../../execution/M3-O1.md)) states: "These fields are **not** available through the `linear-client` domain layer, confirming M1-D2's audit and the need for the raw-adapter path." With v1.1.0, per-issue comment metadata IS available at the domain layer (`Comment.updated_at`, `.resolved_at`, `.parent`, `.children`). The M3-O1 core decision (canonical input accepted) is unchanged, but the integration-path assertion is now factually incorrect for per-issue reads. The CR correctly does not reopen M3-O1 (done-ticket rule), but does not note that M5-D1's scope should include verifying whether the per-issue domain-layer comment surface in v1.1.0 changes the integration path for single-issue reads vs. the batched refresh path. | [docs/execution/M3-O1.md:143–146](../../execution/M3-O1.md), [CR-26.04.07.md §"v1.1.0 Gap Closure Analysis" row 2](CR-26.04.07.md) — "Per-issue comment metadata is domain-layer. The raw-GQL helper is still needed for the batched refresh-cursor pass" | M5-D1 might not realize it should verify the per-issue vs. batched integration-path split for comment metadata if the M3-O1 artifact still asserts the domain layer is entirely insufficient. The gap closure table in the CR implies this question, but M5-D1's scope description does not call it out. | Add to the M5-D1 detailed notes that the audit should verify whether per-issue domain-layer comment reads in v1.1.0 change the integration path relative to the M3-O1 raw-adapter-only conclusion, and clarify the boundary between per-issue domain-layer reads and batched raw-GQL refresh reads. |
| CR-26.04.07-R4 | Low | Todo | Plan-note accuracy | The M3-O1 detailed notes in the active plan ([docs/implementation-plan.md:439–440](../../implementation-plan.md)) state "M1-D2 confirmed that the installed linear-client comment surface does not expose the fields required by the M1-D3 comments_signature contract." This was true for v1.0.0 but is no longer accurate for v1.1.0. The CR proposes updating M5-1 notes (§2) but not M3-O1 notes. Since M3-O1 is Done, a brief factual annotation is a clerical correction, not a material amendment. | [docs/implementation-plan.md:439–440](../../implementation-plan.md), [CR-26.04.07.md §"Proposed Active-Plan Changes"](CR-26.04.07.md) — M3-O1 notes are not listed among proposed updates | A reader of the active plan after this amendment would see M3-O1 notes asserting the domain layer lacks comment metadata, contradicting the v1.1.0 reality and the CR's own gap closure analysis. This is a minor accuracy issue since M3-O1 is Done and its notes are historical, but it could confuse a future session. | Add to the proposed amendment changes a clerical annotation on the M3-O1 detailed notes — for example, appending: "Note: `linear-client` v1.1.0 now exposes per-issue comment metadata at the domain layer; see [CR-26.04.07](planning/change-requests/CR-26.04.07.md). The batched refresh path still requires raw-GQL." |

## Reviewer Notes

### Scope Fit

The amendment is correctly scoped as a material amendment under planning-model
§5: it adds a new named design ticket (M5-D1), changes the dependency version,
and changes the dependency list of an existing ticket (M5-1). The done-ticket
amendment rule is respected — M1-D2 is preserved as historical reference and
not retroactively expanded, and the new work is a follow-on design ticket that
produces its own standalone deliverable.

### Candidate Decisions

All five candidate decisions are well-reasoned:

- Staying on v1.0.0 is correctly rejected — writing raw-GQL helpers that the
  domain layer now handles would be wasted work.
- Patching M1-D2 in place is correctly rejected — rewriting a completed
  ticket's artifact to describe a different library version would violate the
  done-ticket rule.
- Producing a new standalone audit (M5-D1) rather than in-place patching is
  the right structural choice.

### Gap Closure Analysis

The five-gap table is thorough and its v1.1.0 status assessments are consistent
with the linear-client release notes. The per-issue vs. batched distinction is
correctly drawn for comment metadata and relation metadata.

### Application Pathway

The dependency chain M5-D1 -> M5-1 is sound for the gateway implementation
itself. The concern in CR-26.04.07-R1 is about the edges: v1.1.0 consequences
that affect code outside the gateway (renderer, types, error handling) need a
stated mechanism. The user's question — "how are the findings of that new
design task being applied?" — is valid. Some consequences are genuinely
unpredictable until M5-D1 completes; others (the breaking change to
`Issue.get_comments()` root-thread projection, `User.app` replacing
`is_app_actor()`) are already known from the CR's analysis and should be
traceable to specific tickets now.

---

## Review Pass 2

This pass was performed as an independent Stage 2 review with a strict focus on
whether every outdated deliverable produced by done tickets is either updated as
part of the pre-amendment preparation, tracked by a new task, or explicitly
dispositioned.

### Findings (pass 2)

| ID | Severity | Status | Area | Finding | Evidence | Impact | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CR-26.04.07-R5 | Medium | Todo | Outdated deliverable | `docs/design/dependency-map.md` (M4.2-2 deliverable, Done) hardcodes `v1.0.0` as the pinned version for `linear-client` on line 139. The CR's pre-amendment preparation lists README.md, `docs/design/linear-client.md`, and workspace setup documentation as already updated from v1.0.0 to v1.1.0, but does not include `dependency-map.md`. This is a maintained current-state artifact describing the live codebase — not a historical snapshot — and it now disagrees with the installed dependency version. | [docs/design/dependency-map.md:139](../../design/dependency-map.md) — `\| linear-client \| ... \| v1.0.0 \| ...`; [CR-26.04.07.md §"Drafting Notes"](CR-26.04.07.md) — pre-amendment preparation list omits `dependency-map.md` | A future session consulting the dependency map for gateway work (M5-D1 or M5-1) will see `v1.0.0` as the pinned version, contradicting every other reference in the repository. The maintained artifact drifts from the actual installed version. | Add the `docs/design/dependency-map.md` version update to the pre-amendment preparation list. This is a clerical correction — update `v1.0.0` to `v1.1.0` on line 139 and note the entry-point surface changes if any (e.g. new `linear_client.types` exports). |
| CR-26.04.07-R6 | Medium | Todo | Outdated deliverable | M4.2-1 (Done) defined `NewType` aliases in `src/context_sync/_types.py` (`IssueId`, `IssueKey`, `WorkspaceId`, `WorkspaceSlug`, `CommentId`, `AttachmentId`, `WriterId`, `Timestamp`) when no upstream semantic types existed. v1.1.0 introduces 18 `NewType` aliases from `linear_client.types` covering overlapping concepts. The CR's capability table says "Adopt in context-sync's own `_types.py` for cross-boundary type safety" but this obligation is not tracked as an M5-D1 scope item, an M5-1 scope item, or a separate mechanical task. When M5-1 implements the real gateway, it will need to convert between context-sync types and upstream types at the gateway boundary — or adopt the upstream types directly — but neither the M5-D1 audit scope nor the M5-1 ticket notes mention this reconciliation. | [src/context_sync/_types.py](../../../src/context_sync/_types.py) — 8 domain aliases; [CR-26.04.07.md §"Additional v1.1.0 Capabilities"](CR-26.04.07.md) — "Adopt in context-sync's own `_types.py`" listed under "Potential use" but untracked | The gateway boundary will accept upstream-typed values (e.g. `linear_client.types.IssueId`) and must return context-sync-typed values (e.g. `context_sync._types.IssueId`). Without an explicit reconciliation plan, M5-1 will either silently cast between them (losing type safety), or ad-hoc decide a reconciliation strategy that M5-D1 should have evaluated. | Add to M5-D1 detailed notes that the audit must compare the upstream `linear_client.types` aliases against `_types.py` and recommend a reconciliation strategy: adopt upstream types, re-export them, or maintain separate types with explicit conversion at the gateway boundary. The chosen strategy should be part of the adapter-boundary definition. |
| CR-26.04.07-R7 | Medium | Todo | Operational precondition | M5-D1 is described as "Inspect the installed `linear-client` v1.1.0 surface" but has `None` for dependencies and no operational precondition ensuring v1.1.0 is actually installed in the repo-local `.venv`. M1-O1 (Done) established the `.venv` with v1.0.0. The CR's pre-amendment preparation updated documentation references but does not confirm the actual installed package was upgraded, nor does it add an operational note or prerequisite for M5-D1. | [CR-26.04.07.md §"Proposed Active-Plan Changes" §1](CR-26.04.07.md) — M5-D1 dependencies: `None`; [docs/execution/M1-O1.md](../../execution/M1-O1.md) — original environment bootstrap was v1.0.0; [CR-26.04.07.md §"Drafting Notes"](CR-26.04.07.md) — pre-amendment prep does not mention `.venv` upgrade | If v1.1.0 is not actually installed when M5-D1 starts, the audit will inspect v1.0.0 and produce incorrect findings. The design ticket's correctness depends on an operational state that is not verified. | Either (a) add a brief operational precondition note to the M5-D1 detailed notes requiring the executor to verify that `linear-client` v1.1.0 is the installed version before auditing begins, or (b) confirm in the pre-amendment preparation section that the `.venv` was already upgraded and record that confirmation explicitly. |
| CR-26.04.07-R8 | Low | Todo | Factual accuracy | The "Additional v1.1.0 Capabilities" table's "100% type completeness" row recommends "Remove `# type: ignore[import-untyped]` from `_cli.py`" as a potential use. No such suppression comment exists in the current code — [src/context_sync/_cli.py:185](../../../src/context_sync/_cli.py) imports `from linear_client import Linear` without any type-ignore annotation. The suggested action references a non-existent code pattern. | `rg 'type: ignore' src/context_sync/_cli.py` — zero matches; [_cli.py:185](../../../src/context_sync/_cli.py) — bare import with no suppression | A future session executing M5-D1 or M5-1 might search for a `# type: ignore[import-untyped]` that does not exist, causing confusion. Minor issue since the table is informational. | Correct the "Potential use" cell for the "100% type completeness" row. If `linear-client` v1.0.0 already shipped `py.typed`, the benefit is already realized and the row should say so. If v1.0.0 did not ship `py.typed`, adjust the description to note that the import already works without a suppression comment under the current Pyright configuration. |

### Reviewer Notes (pass 2)

#### Outdated Deliverables Assessment

The user specifically asked whether all outdated deliverables from done tickets
receive new tasks or updates. Summary of the assessment:

| Done deliverable | v1.1.0 impact | CR disposition | Gap |
| --- | --- | --- | --- |
| `docs/design/linear-domain-coverage-audit-v1.0.0.md` (M1-D2) | Superseded — audited v1.0.0 surface | New M5-D1 ticket produces v1.1.0 replacement | None |
| `docs/execution/M3-O1.md` (M3-O1) | Factually incorrect assertion about domain-layer comment metadata | Noted by pass 1 R3; M5-D1 scope suggestion | Adequate — R3 recommends M5-D1 verification |
| Implementation plan M3-O1 notes (lines 439–440) | Assert domain layer lacks comment metadata | Noted by pass 1 R4; clerical annotation | Adequate — clerical fix, not a new task |
| `docs/design/dependency-map.md` (M4.2-2) | Hardcodes `v1.0.0` | **Not addressed** | **R5** |
| `src/context_sync/_types.py` (M4.2-1) | Overlapping upstream types now available | Mentioned in capability table but untracked | **R6** |
| Implementation plan M5-1 notes (lines 900–923) | Reference v1.0.0 boundary and version | CR proposes updating | None |
| Implementation plan risk register (line 1146) | References audit filename | CR proposes updating | None |

#### Existing Findings Still Live

All four pass 1 findings (R1–R4) remain `Todo` and are still valid. Pass 2
findings reinforce pass 1 themes:

- R5 extends R2's traceability concern to a specific maintained artifact the CR
  missed.
- R6 extends R2's disposition-mapping concern to a concrete implementation
  obligation at the gateway type boundary.
- R7 extends R1's scope-completeness concern to the operational precondition
  for M5-D1.

#### Overall Assessment

The CR is well-structured and correctly applies the done-ticket amendment rule.
The gap-closure analysis is thorough, the candidate decisions are sound, and the
M5-D1 / M5-1 dependency chain is the right structural choice. The primary
weakness is that the CR treats its capability and breaking-change tables as
informational context without mapping their consequences to concrete tasks or
disposition categories. Pass 1 R2 flagged this at the table level; pass 2 R5
and R6 identify specific deliverables where that gap produces tracked
obligations that the CR does not account for.
