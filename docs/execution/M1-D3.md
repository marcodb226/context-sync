# M1-D3 - Refresh composite freshness contract amendment

> **Status**: Phase A complete; pending independent Phase B review
> **Plan ticket**:
> [M1-D3](../implementation-plan.md#m1-d3---refresh-composite-freshness-contract-amendment)
> **Prerequisite**:
> [M1-D1](../implementation-plan.md#m1-d1---refresh-freshness-validation-spike)
> **Primary governing artifacts**:
> [docs/implementation-plan.md](../implementation-plan.md),
> [docs/policies/common/execution-model.md](../policies/common/execution-model.md),
> [docs/adr.md](../adr.md#52-refresh-incremental-whole-snapshot-update),
> [docs/adr.md](../adr.md#oq-1-refresh-freshness-validation-against-live-linear-behavior),
> [docs/design/0-top-level-design.md](../design/0-top-level-design.md#62-refresh-flow),
> [docs/design/refresh-freshness-validation.md](../design/refresh-freshness-validation.md),
> [docs/design/linear-client.md](../design/linear-client.md),
> [docs/future-work.md](../future-work.md#fw-2-attachment-content-handling)

## Initial Questions

None

## Work Log

- Opened the ticket execution artifact and verified the governing plan is the
  active [docs/implementation-plan.md](../implementation-plan.md) and that
  [M1-D3](../implementation-plan.md#m1-d3---refresh-composite-freshness-contract-amendment)
  is a design ticket under the active execution model.
- Re-read the ticket notes in
  [docs/implementation-plan.md](../implementation-plan.md#m1-d3---refresh-composite-freshness-contract-amendment),
  the negative spike result in
  [docs/design/refresh-freshness-validation.md](../design/refresh-freshness-validation.md),
  the current refresh design in
  [docs/design/0-top-level-design.md](../design/0-top-level-design.md#62-refresh-flow),
  the current ADR refresh strategy in
  [docs/adr.md](../adr.md#52-refresh-incremental-whole-snapshot-update),
  the `linear-client` boundary guidance in
  [docs/design/linear-client.md](../design/linear-client.md),
  and the attachment deferral in
  [docs/future-work.md](../future-work.md#fw-2-attachment-content-handling)
  to scope the amendment.
- Confirmed that the pre-amendment ADR and top-level design still described the
  invalidated single-cursor refresh design. Chose to rewrite both the
  governing design section and the ADR refresh-strategy prose so later readers
  do not have to reconstruct the accepted contract only from the
  [OQ-1](../adr.md#oq-1-refresh-freshness-validation-against-live-linear-behavior)
  outcome note.
- Chose the v1 composite refresh cursor as three explicit components:
  `issue_updated_at`, `comments_signature`, and `relations_signature`.
  Recorded that local ticket files persist those values under a
  machine-readable `refresh_cursor` mapping and that `refresh` compares the
  local and remote cursors by exact equality rather than by a monotonic
  `updated_at > last_synced_at` test.
- Defined the comment freshness contract conservatively as a deterministic
  digest over visible comment/thread metadata needed by the rendered comments
  section: stable comment IDs, parent/root relationships, comment `updated_at`,
  and per-thread `resolved` state. Clarified that the digest is computed
  locally from remote metadata rather than supplied by Linear as an API-level
  checksum. This satisfies the plan requirement to detect comment creation and
  comment edits, treats comment deletion as explicit best-effort scope when no
  reliable deletion signal exists, and keeps thread-state changes inside the
  same cursor component.
- Explicitly chose not to run additional live relation probes in this ticket.
  The plan notes treat relation probing as recommended input rather than a
  mandatory subtask, so the amendment instead documents a conservative
  first-release rule: relation freshness is an independent cursor component and
  must not rely on an unverified assumption that relation changes advance the
  parent issue `updated_at`.
- Defined relation freshness as a deterministic digest over the visible
  persisted relation tuples that affect rendered frontmatter and graph
  reachability. Recorded that the canonical input must include dimension,
  relation type, and target identity, using stable target UUIDs for canonical
  ordering when available while still including the current human-readable
  target key because that value is rendered locally.
- Narrowed attachment freshness out of the v1 selective-refresh correctness
  contract while keeping attachment metadata persisted in ticket files.
  Recorded that attachment-only drift is not guaranteed to be detected during
  selective refresh and remains deferred to
  [FW-2](../future-work.md#fw-2-attachment-content-handling).
- Recorded the exact remote-data requirements that
  [M1-D2](../implementation-plan.md#m1-d2---linear-domain-coverage-audit-and-adapter-boundary)
  must audit: issue identity plus issue `updated_at`, comment/thread metadata
  sufficient to build `comments_signature` without downloading full comment
  bodies during freshness checks, and relation metadata sufficient to build
  `relations_signature`, all behind one narrow batched metadata adapter path
  instead of one full ticket fetch per tracked issue.
- Updated
  [docs/design/0-top-level-design.md](../design/0-top-level-design.md#62-refresh-flow)
  to replace the invalidated single-cursor refresh flow with the accepted
  composite-cursor design and to record the explicit `M1-D2` audit inputs.
- Updated
  [docs/adr.md](../adr.md#52-refresh-incremental-whole-snapshot-update)
  so the ADR refresh strategy now matches the accepted composite-cursor
  contract instead of leaving the stale issue-level-only design in place.
- Updated
  [docs/design/refresh-freshness-validation.md](../design/refresh-freshness-validation.md)
  to add a forward pointer from the historical spike outcome to the accepted
  [M1-D3](../implementation-plan.md#m1-d3---refresh-composite-freshness-contract-amendment)
  amendment so the old release-gate artifact no longer reads like an open
  blocker in isolation.
- Updated the
  [M1-D2](../implementation-plan.md#m1-d2---linear-domain-coverage-audit-and-adapter-boundary)
  detailed notes in
  [docs/implementation-plan.md](../implementation-plan.md)
  to make the audit responsible for surfacing any cost risk in the accepted
  metadata-only comment freshness path. If the batched metadata route proves
  materially more expensive than intended, the note now says that later work
  may propose an explicit degraded append-only fast mode, but must not silently
  replace the default correctness-first refresh contract.
- Updated [docs/future-work.md](../future-work.md#fw-2-attachment-content-handling)
  to link the attachment-freshness deferral back to
  [M1-D3](../implementation-plan.md#m1-d3---refresh-composite-freshness-contract-amendment)
  for bidirectional traceability.
- After the independent Phase B review and accepted owner-response
  dispositions, tightened the governing contract further to resolve the
  accepted fix-now findings:
  `sync` now explicitly persists `refresh_cursor`,
  `refresh` now treats missing/partial/invalid or format-incompatible local
  cursors as stale rather than as fresh,
  the digest format is now specified as SHA-256 over canonical UTF-8 records
  with a normative `v1:` prefix,
  the canonical ordering rules for both signature components are now explicit,
  the thread-level `resolved` flag now has an explicit thread-record mapping in
  `comments_signature`,
  and
  [M1-D2](../implementation-plan.md#m1-d2---linear-domain-coverage-audit-and-adapter-boundary)
  now explicitly owns validating the assumption that comment-level
  `updated_at` advances on comment edits or else recording the required
  fallback.

## Completion Summary

- Documents checked for design alignment:
  [docs/implementation-plan.md](../implementation-plan.md),
  [docs/adr.md](../adr.md#52-refresh-incremental-whole-snapshot-update),
  [docs/adr.md](../adr.md#oq-1-refresh-freshness-validation-against-live-linear-behavior),
  [docs/design/0-top-level-design.md](../design/0-top-level-design.md#62-refresh-flow),
  [docs/design/refresh-freshness-validation.md](../design/refresh-freshness-validation.md),
  [docs/design/linear-client.md](../design/linear-client.md),
  [docs/future-work.md](../future-work.md#fw-2-attachment-content-handling),
  and
  [docs/policies/common/execution-model.md](../policies/common/execution-model.md).
- Deliverables produced:
  an amended governing refresh design in
  [docs/design/0-top-level-design.md](../design/0-top-level-design.md#62-refresh-flow),
  matching ADR refresh-strategy updates in
  [docs/adr.md](../adr.md#52-refresh-incremental-whole-snapshot-update),
  a follow-on resolution pointer in
  [docs/design/refresh-freshness-validation.md](../design/refresh-freshness-validation.md),
  an added audit note in
  [docs/implementation-plan.md](../implementation-plan.md#m1-d2---linear-domain-coverage-audit-and-adapter-boundary)
  covering potential degraded fast-mode follow-up if the metadata-only comment
  freshness path is too costly,
  a second added audit note in
  [docs/implementation-plan.md](../implementation-plan.md#m1-d2---linear-domain-coverage-audit-and-adapter-boundary)
  covering validation of the comment-level `updated_at` assumption,
  a file-format note for `refresh_cursor` in
  [docs/adr.md](../adr.md#2-persistence-format),
  a bidirectional attachment-deferral traceability link in
  [docs/future-work.md](../future-work.md#fw-2-attachment-content-handling),
  and this execution record.
- Accepted design outcome:
  v1 `refresh` uses a composite per-ticket cursor with
  `issue_updated_at`, `comments_signature`, and `relations_signature`;
  comparison is exact equality against the local `refresh_cursor`;
  missing/partial/invalid local cursors are treated as stale rather than as
  fresh;
  `sync` and other mutating ticket-writing flows persist the current cursor as
  the next refresh baseline;
  signature values use a normative `v1:`-prefixed SHA-256 canonicalization
  contract;
  `last_synced_at` remains informational rather than the staleness test; and
  attachment-only metadata drift is explicitly outside the v1 selective-refresh
  correctness contract. The comment cursor now explicitly covers comment
  creation and edit, plus best-effort deletion detection when the remote
  metadata does not expose a clearer deletion signal.
- ADR update scope decision:
  this ticket **did** rewrite
  [docs/adr.md](../adr.md#52-refresh-incremental-whole-snapshot-update)
  because leaving the stale single-cursor prose in place would make the ADR
  and top-level design disagree about the active refresh contract.
- Conservative-design note:
  no additional live relation probes were run in this ticket. The design makes
  that explicit and resolves the uncertainty by requiring independent relation
  freshness rather than by assuming favorable upstream behavior.
- Validation notes:
  this was a docs-only design ticket, so no repository-wide lint, format, or
  test commands were run. Validation consisted of manual cross-document
  consistency checks across the governing plan, ADR, top-level design,
  freshness-validation artifact, `linear-client` boundary summary, and
  future-work deferral.

## Reviewer Handoff

- Confirm that
  [docs/design/0-top-level-design.md](../design/0-top-level-design.md#62-refresh-flow)
  and
  [docs/adr.md](../adr.md#52-refresh-incremental-whole-snapshot-update)
  now agree on the accepted first-release refresh contract and no longer leave
  the invalidated issue-level-only design as live guidance.
- Confirm that the local metadata/comparison contract is explicit enough for
  later implementation:
  ticket files store `refresh_cursor`,
  the cursor has three components,
  and `refresh` uses exact equality rather than `updated_at > last_synced_at`.
- Confirm that the comment freshness component is specific enough to support
  comment creation and comment edits, treats comment deletion as explicit
  best-effort scope when no reliable deletion signal exists, and that the
  relation freshness component is conservative enough to keep graph state
  correct without requiring this ticket to reopen live probing work.
- Confirm that the accepted design is explicit that `comments_signature` is a
  locally computed digest over batched metadata inputs, not an API-provided
  checksum, and that the intended freshness path avoids downloading full
  comment bodies just to decide staleness.
- Confirm that attachment-only freshness is explicitly narrowed out of the v1
  selective-refresh correctness contract and is visibly deferred to
  [FW-2](../future-work.md#fw-2-attachment-content-handling).
- Confirm that the remote-data requirements recorded for
  [M1-D2](../implementation-plan.md#m1-d2---linear-domain-coverage-audit-and-adapter-boundary)
  are concrete enough for the adapter-boundary audit to proceed without
  rediscovering the refresh inputs ad hoc.
