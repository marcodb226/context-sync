# M5-2 - Supported public runtime validation and smoke path

> **Status**: Phase A complete
> **LLM**: opus-4.6
> **Effort**: N/A
> **Time spent**: ~40m
> **Plan ticket**: [M5-2](../implementation-plan.md#m5-2---supported-public-runtime-validation-and-smoke-path)
> **Dependencies**: [M5-1](../implementation-plan.md#m5-1---real-linear-gateway-and-runtime-wiring) (Done), [M4-2](../implementation-plan.md#m4-2---operational-logging-validation-hardening-and-user-docs) (Done), [M4.2-2](../implementation-plan.md#m4.2-2---coverage-tooling-agent-awareness-artifacts-and-interface-documentation) (Done)
> **Primary governing artifacts**:
> [docs/implementation-plan.md](../implementation-plan.md),
> [docs/policies/common/execution-model.md](../policies/common/execution-model.md),
> [docs/execution/M4-2-review.md](M4-2-review.md),
> [src/context_sync/_cli.py](../../src/context_sync/_cli.py),
> [src/context_sync/_sync.py](../../src/context_sync/_sync.py),
> [src/context_sync/_real_gateway.py](../../src/context_sync/_real_gateway.py),
> [src/context_sync/_testing.py](../../src/context_sync/_testing.py),
> [README.md](../../README.md)

## Initial Questions

None. All three dependencies are satisfied (`Done`). The ticket scope is clear
from the plan notes and the deferred findings in
[M4-2-R2](M4-2-review.md) and [M4-2-R3](M4-2-review.md).

## Work Log

- Verified governing plan is active at
  [docs/implementation-plan.md](../implementation-plan.md) and all three M5-2
  dependencies are `Done`.
- Updated active-plan status row from `Todo` to `In progress`.
- Read the deferred findings:
  - [M4-2-R2](M4-2-review.md): the existing "end-to-end" suite does not
    exercise `main()`, parser construction, `--log-level`, console-script
    dispatch, or the real `linear=` constructor path. Tests import private
    `_run_*` helpers and inject `_gateway_override`.
  - [M4-2-R3](M4-2-review.md): no manual smoke-test procedure or recorded
    results exist in repository docs.
- Read existing test structure:
  [tests/test_cli.py](../../tests/test_cli.py) (parser tests, exit codes,
  handler integration via private `_run_*` with `_gateway_override`),
  [tests/test_e2e.py](../../tests/test_e2e.py) (library pipeline via private
  handlers, honestly classified as component tests after M4-2 Phase C).
- Designed the mock `Linear` transport double pattern: a `MagicMock` with
  `issue()` factory (supports `id=` and `key=` kwargs), `gql.query()` and
  `gql.paginate_connection()` async mocks. The mock is wired directly through
  `ContextSync(linear=mock_linear)` → `RealLinearGateway(mock_linear)` — no
  `_gateway_override` is used.
- Wrote 42 tests in
  [tests/test_public_surface.py](../../tests/test_public_surface.py) (527
  code lines per `cloc`):
  - **CLI integration through `main()`** (11 tests): `sync` (with ticket, JSON,
    standalone rebuild), `refresh` (after sync, JSON), `remove` (after sync,
    JSON), `diff` (after sync, JSON), `--log-level DEBUG`, `--log-level OFF`.
    All route through `build_parser()` → `main()` → `_create_linear_client`
    (patched) → handler → `ContextSync(linear=...)` → `RealLinearGateway`.
  - **Library entry point via `ContextSync(linear=...)`** (7 tests): `sync`,
    `refresh`, `remove`, `diff` through the real gateway wiring; UUID vs key
    selector routing; constructor validation error.
  - **Failure-contract regression** (20 tests): text-mode error output for 9
    `ContextSyncError` subtypes + `ValueError`, JSON-mode error output for 9
    `ContextSyncError` subtypes + `ValueError`. Each test verifies exit code 1
    and the error type/message in the appropriate output stream.
  - **Bootstrap failure paths** (4 tests): missing `linear-client` (text +
    JSON), auth failure (text + JSON).
- Removed the pre-release runtime warning from [README.md](../../README.md)
  (lines 22–27). The real `linear-client`-backed gateway adapter is now wired
  and tested since [M5-1](../implementation-plan.md#m5-1---real-linear-gateway-and-runtime-wiring).
- Added a smoke-validation recipe to [README.md](../../README.md) under
  `### Smoke validation` in the Operational guidance section. Covers:
  - Happy path: `sync` → `refresh` → `diff` → `remove` cycle with expected
    stdout/exit-code behavior.
  - Failure path: missing credentials, invalid ticket key, JSON error output.
- Validation: `scripts/validate.sh` passes all gates — ruff lint (0 errors),
  ruff format (43 files clean), pyright (0 errors), pytest (559 passed, 92%
  branch coverage).
- Coding-guidelines compliance check: no magic numbers, no files over 1000
  code lines (new file: 527 lines), all interfaces documented, no hardcoded
  secrets, structured error types used throughout.
- Terminology check: no banned terms in any changed file.
- No changelog entry required (pre-1.0.0 release).
- No temporary scaffolding introduced.

## Completion Summary

### What was produced

1. **[tests/test_public_surface.py](../../tests/test_public_surface.py)** — New
   test module with 42 tests exercising the supported public runtime through
   `main()` (CLI) and `ContextSync(linear=...)` (library). Uses a maintained
   mock `Linear` transport double that routes through `RealLinearGateway`
   without `_gateway_override`. 527 code lines per `cloc`. Covers CLI dispatch
   for all four subcommands, UUID vs key selector routing, log-level wiring,
   JSON and text failure-contract regression for all error types, and bootstrap
   failure paths.

2. **[README.md](../../README.md)** — Pre-release runtime warning removed. Smoke
   validation recipe added under `### Smoke validation` covering one happy-path
   cycle and representative failure scenarios with expected stdout/stderr
   behavior.

### Validations performed

- `scripts/validate.sh`: all gates pass (ruff lint, ruff format, pyright,
  pytest with coverage).
- 559 tests pass, 0 failures.
- 92% overall branch coverage.

### Follow-up items

- Live-workspace smoke validation against a credentialed Linear environment
  was not performed in this session. The durable smoke recipe in
  [README.md](../../README.md) is the reference for that verification.
- The existing [tests/test_e2e.py](../../tests/test_e2e.py) and
  [tests/test_cli.py](../../tests/test_cli.py) handler-integration tests
  remain as component coverage via `_gateway_override`. The new
  [tests/test_public_surface.py](../../tests/test_public_surface.py) is the
  supported public-surface proof.
