# M5-3 - CLI auth-mode selection

> **Status**: Phase C complete; all review findings addressed
> **LLM**: opus-4.6
> **Effort**: N/A
> **Time spent**: ~25m (Phase A), ~20m (Phase C)
> **Plan ticket**: [M5-3](../implementation-plan.md#m5-3---cli-auth-mode-selection)
> **Dependencies**: [M5-2](../implementation-plan.md#m5-2---supported-public-runtime-validation-and-smoke-path) (Done)
> **Primary governing artifacts**:
> [docs/implementation-plan.md](../implementation-plan.md),
> [docs/policies/common/execution-model.md](../policies/common/execution-model.md),
> [src/context_sync/_cli.py](../../src/context_sync/_cli.py),
> [tests/test_cli.py](../../tests/test_cli.py),
> [tests/test_public_surface.py](../../tests/test_public_surface.py),
> [README.md](../../README.md)

## Initial Questions

None. The sole dependency [M5-2](../implementation-plan.md#m5-2---supported-public-runtime-validation-and-smoke-path) is `Done`. The ticket scope is clear from the plan notes and [LC-2](../linear-client-issues.md#lc-2---no-environment-variable-control-for-auth_mode).

## Work Log

1. Verified governing plan is active at [docs/implementation-plan.md](../implementation-plan.md) and the sole dependency [M5-2](../implementation-plan.md#m5-2---supported-public-runtime-validation-and-smoke-path) is `Done`.
2. Updated active-plan status row from `Todo` to `In progress`.
3. Read the `Linear.__init__` constructor signature in `linear-client` to confirm the `auth_mode` parameter type: `Literal["oauth", "client_credentials", "api_key"]` with default `"oauth"`.
4. Read [LC-2](../linear-client-issues.md#lc-2---no-environment-variable-control-for-auth_mode) — the library intentionally does not provide env-var control; callers select the policy.
5. Implemented in [src/context_sync/_cli.py](../../src/context_sync/_cli.py):
   - Added `AUTH_MODE_CHOICES` constant and `_AuthMode` Literal type alias.
   - Added `--auth-mode` global option to `build_parser()` (choices: `oauth`, `client_credentials`, `api_key`; default: `None` for env inference).
   - Updated `_create_linear_client(auth_mode)` to accept the mode and infer from environment when `None`: `LINEAR_API_KEY` -> `api_key`, `LINEAR_CLIENT_ID` -> `client_credentials`, otherwise `oauth`.
   - Updated all four handler functions (`_run_sync`, `_run_refresh`, `_run_remove`, `_run_diff`) to pass `args.auth_mode` to `_create_linear_client()`.
6. Added parser tests in [tests/test_cli.py](../../tests/test_cli.py) — 6 new tests in `TestParserConstruction`: default is `None`, all three valid modes accepted, invalid mode rejected, choices match constant.
7. Added env-inference unit tests in [tests/test_cli.py](../../tests/test_cli.py) — 5 new tests in `TestAuthModeEnvInference`: each env var correctly infers its mode, `LINEAR_API_KEY` takes precedence over `LINEAR_CLIENT_ID`, explicit mode overrides env.
8. Added handler passthrough tests in [tests/test_cli.py](../../tests/test_cli.py) — 4 new tests in `TestAuthModeHandlerPassthrough`: explicit mode passed through `main()`, `None` passed when omitted, mode reaches sync and diff handlers.
9. Added public-surface integration tests in [tests/test_public_surface.py](../../tests/test_public_surface.py) — 5 new tests in `TestCliMainAuthMode`: explicit `api_key` and `client_credentials` modes reach `Linear()`, env inference for both modes works through `main()`, explicit mode overrides env through `main()`.
10. Updated [README.md](../../README.md):
    - Added `--auth-mode MODE` to the global options table with env-inference description.
    - Added "Auth-mode inference" explanatory paragraph after the table.
    - Expanded smoke recipe to two happy-path sections: one for `api_key` mode and one for `client_credentials` mode (with explicit `--auth-mode` flag).
11. Coding guidelines compliance check — verified against [docs/policies/common/coding-guidelines.md](../policies/common/coding-guidelines.md) and [docs/policies/common/python/coding-guidelines.md](../policies/common/python/coding-guidelines.md):
    - No `typing.Any` in new signatures (parameter is `_AuthMode | None`).
    - Full docstring with Parameters, Returns, and Raises sections.
    - No magic numbers or opaque structural assumptions.
    - Cross-references in Markdown are clickable links.
    - Terminology policy checked — no banned terms used.
12. No changelog entry required — version is `0.1.0.dev0` (pre-stable).
13. Validation: `ruff check` clean, `ruff format --check` clean, `pyright` 0 errors, `pytest` 581/581 passed (20 new tests).

## Completion Summary

**Produced (Phase A):**

- `--auth-mode` global CLI option with fixed `client_credentials` default.
- `_AuthMode` type alias, `AUTH_MODE_CHOICES` constant, and `_DEFAULT_AUTH_MODE` constant in [src/context_sync/_cli.py](../../src/context_sync/_cli.py).
- 16 new automated tests across [tests/test_cli.py](../../tests/test_cli.py) (13 tests) and [tests/test_public_surface.py](../../tests/test_public_surface.py) (3 tests).
- Updated [README.md](../../README.md) global options table, credential setup, and expanded smoke recipe covering `client_credentials` (default) and `api_key` modes.

**Phase C fixes ([M5-3-R1](M5-3-review.md#findings)):**

- Replaced env-inference heuristic with a fixed `client_credentials` default (plan line 1116).
- Set argparse `--auth-mode` default to `client_credentials` instead of `None`.
- `_create_linear_client()` resolves `None` to `client_credentials` via `_DEFAULT_AUTH_MODE`.
- Removed `import os` (no longer needed without env probing).
- Reworked `TestAuthModeEnvInference` (5 tests) → `TestAuthModeFixedDefault` (3 tests).
- Reworked env-inference tests in `TestCliMainAuthMode` → fixed-default assertions.
- Updated README: credential setup leads with `client_credentials`, `api_key` shown as explicit-flag alternative, smoke recipe reordered with default mode first.

**Validations performed (Phase C):**

- `ruff check src/ tests/` — all checks passed.
- `ruff format --check src/ tests/` — all files formatted.
- `pyright` — 0 errors, 0 warnings.
- `pytest -v` — 577/577 passed in 3.99s.

**No temporary scaffolding introduced.**

**No changelog entry required** (`0.1.0.dev0`).

**Follow-up items:** None.
