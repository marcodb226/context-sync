# M4-2 — Operational Logging, Validation Hardening, and User Docs

> **Status**: Phase A — Complete

## Initial Questions

None.

## Work Log

### 2026-03-22 — Analysis and implementation

**Scope from plan:** Add the INFO/DEBUG logging contract, end-to-end validation
coverage, onboarding and usage docs, and the sample configuration artifact
needed to satisfy the repository's documentation/security conventions.

**Dependencies satisfied:**
- [M4-1](../implementation-plan.md#m4-1---cli-surface-and-command-output-contracts) — Done (Phase C complete)

**Design references read:**
- [docs/adr.md §6.1](../adr.md#61-snapshot-consistency-contract) — logging contract
- [docs/design/0-top-level-design.md](../design/0-top-level-design.md) — error handling, logging, validation
- [docs/policies/common/coding-guidelines.md](../policies/common/coding-guidelines.md) — documentation, security, logging
- [docs/policies/common/python/coding-guidelines.md](../policies/common/python/coding-guidelines.md) — Python conventions
- [docs/policies/common/cli-conventions.md](../policies/common/cli-conventions.md) — CLI logging conventions

**Logging audit findings:**

ADR §6.1 requires specific INFO and DEBUG level logging. Audit of existing code
identified these gaps:

1. `sync` completed log was missing `unchanged` count.
2. `refresh` started log was missing `max_tickets_per_root` and used hardcoded
   `"refresh:"` instead of the `snapshot_mode` parameter (so `add`/`remove-root`
   runs incorrectly showed as "refresh:").
3. `refresh` completed log was missing `roots_at_cap` count and also used
   hardcoded `"refresh:"`.
4. `diff` had no start or end INFO logs at all.
5. No DEBUG log for clean lock acquisition.
6. No per-ticket DEBUG logs for fresh/stale decisions during refresh.

**Key decisions:**

1. Extracted the diff pipeline body (~170 code lines) from
   [src/context_sync/_sync.py](../../src/context_sync/_sync.py) into a new
   `run_diff()` async function in
   [src/context_sync/_diff.py](../../src/context_sync/_diff.py) to keep
   `_sync.py` under the 1,000 code-line limit after adding logging.
2. Used `snapshot_mode` parameter in refresh started/completed logs so that
   `add` and `remove-root` operations (which delegate to `_refresh_under_lock`)
   correctly identify themselves in log output.
3. The sample configuration artifact already existed at
   [scripts/.linear_env.sh.sample](../../scripts/.linear_env.sh.sample) and
   satisfies the coding-guidelines "repo-local alternative" requirement. No new
   artifact was needed.

### 2026-03-22 — Implementation

**Files created:**
- [tests/test_e2e.py](../../tests/test_e2e.py) — 16 end-to-end tests covering
  all five CLI modes (sync, refresh, add, remove-root, diff), logging contract
  verification (INFO and DEBUG levels), JSON output validation, multi-root
  scenarios, quarantine/recovery, and idempotency.

**Files modified:**
- [src/context_sync/_sync.py](../../src/context_sync/_sync.py) — added
  `unchanged` to sync completed log; parameterized refresh started/completed
  logs with `snapshot_mode` and added `max_tickets_per_root`/`roots_at_cap`;
  added per-ticket DEBUG logs for stale/fresh decisions during refresh; added
  `diff` INFO start/end logs; extracted diff body to `_diff.py`.
- [src/context_sync/_diff.py](../../src/context_sync/_diff.py) — received the
  diff pipeline body as `run_diff()` async function with complete logging;
  updated module docstring and imports.
- [src/context_sync/_lock.py](../../src/context_sync/_lock.py) — added DEBUG
  log for clean lock acquisition.
- [README.md](../../README.md) — added CLI usage section (commands, global
  options, per-command options, exit codes, output modes), configuration section
  (environment variables table referencing the sample artifact), and operational
  guidance section (logging, lock handling, missing-root policy, common
  failures).
- [docs/implementation-plan.md](../implementation-plan.md) — updated M4-2
  status to `In progress`.

**Validation:**
- `ruff check src/ tests/` — all checks passed.
- `ruff format --check src/ tests/` — all 39 files formatted.
- `pytest tests/` — 466 tests passed (including 16 new e2e tests).
- `cloc` confirms all modified files are under 1,000 code lines:
  - `_sync.py`: 910 code lines
  - `_diff.py`: 204 code lines
  - `_cli.py`: 329 code lines

**Coding-guidelines compliance check:**
- Header docstrings: present on all modified modules.
- No magic numbers: all log format strings use named parameters.
- Repository file references as clickable links: README uses markdown links.
- All interfaces documented: `run_diff()` has full docstring with parameters,
  return types, and raises.
- No hardcoded secrets: no credentials in code or logs.
- Fail loudly: logging does not suppress errors.
- Logging intentional: INFO covers run lifecycle per ADR §6.1; DEBUG covers
  per-ticket decisions. No secrets in log output.
- Sample configuration artifact: already tracked at
  [scripts/.linear_env.sh.sample](../../scripts/.linear_env.sh.sample).
- Supported documentation aligned with behavior: README documents all five CLI
  commands with their options and defaults, matching the implemented parser.
- File sizes under 1,000 code lines.

**Temporary scaffolding:** None introduced.

**No changelog entry required:** The repository version is `0.1.0.dev0`
(pre-stable), so the changelog gate does not apply.

## Completion Summary

**What was produced:**

1. Complete INFO-level logging contract per ADR §6.1: every mode (`sync`,
   `refresh`, `add`, `remove-root`, `diff`) now emits started and completed
   INFO logs with root count, per-root ticket cap, reachable count,
   created/updated/unchanged/removed/error counts, roots-at-cap count, and
   duration.
2. Complete DEBUG-level logging: per-ticket stale/fresh decisions during
   refresh with reason (format_version, missing cursor, cursor mismatch, or
   fresh); clean lock acquisition; existing pruned/renamed ticket logs retained.
3. Lock-handling logs already covered by prior milestones: stale-lock
   preemption (WARNING), diff stale-lock observation (INFO), and diff
   non-stale-lock refusal (DiffLockError with rate-limit explanation).
4. 16 end-to-end fixture tests
   ([tests/test_e2e.py](../../tests/test_e2e.py)) covering all major modes,
   logging contract (INFO and DEBUG), JSON output, multi-root, quarantine/
   recovery, and idempotency.
5. User-facing documentation in [README.md](../../README.md): CLI command
   reference, global and per-command options, exit codes, output modes,
   configuration (environment variables with the sample artifact reference),
   and operational guidance (logging, lock handling, missing-root policy,
   common failures table).
6. Diff pipeline extraction to
   [src/context_sync/_diff.py](../../src/context_sync/_diff.py) brought
   `_sync.py` from 1,033 to 910 code lines.

**Validations performed:**
- Full lint pass (`ruff check`) — clean.
- Full format pass (`ruff format`) — clean.
- Full test suite (466 tests) — all passing.
- Code-line counts within guideline limits.

**Follow-up items:**
- The `_create_linear_client()` function in the CLI still constructs a raw
  `Linear()` instance. When a `RealLinearGateway` adapter is implemented, the
  CLI should construct the gateway and pass it through. This is a known
  limitation noted in the M4-1 execution record and not in scope for M4-2.
