# M5-4 - Adopt `linear-client` NewTypes and eliminate duplicate aliases

> **Status**: Phase C complete; all review findings addressed
> **LLM**: opus-4.6
> **Effort**: N/A
> **Time spent**: ~15m (Phase A)
> **Plan ticket**: [M5-4](../implementation-plan.md#m5-4---adopt-linear-client-newtypes)
> **Dependencies**: [M5-1](../implementation-plan.md#m5-1---real-linear-gateway-and-runtime-wiring) (Done)
> **Primary governing artifacts**:
> [docs/implementation-plan.md](../implementation-plan.md),
> [docs/policies/common/execution-model.md](../policies/common/execution-model.md),
> [src/context_sync/_types.py](../../src/context_sync/_types.py),
> [src/context_sync/_real_gateway.py](../../src/context_sync/_real_gateway.py),
> [docs/linear-client-issues.md](../linear-client-issues.md#lc-6---newtype-identity-mismatch-across-the-boundary)

## Initial Questions

None. The sole dependency [M5-1](../implementation-plan.md#m5-1---real-linear-gateway-and-runtime-wiring) is `Done`. The ticket scope is clear from the plan notes and [LC-6](../linear-client-issues.md#lc-6---newtype-identity-mismatch-across-the-boundary).

## Work Log

1. Verified governing plan is active at [docs/implementation-plan.md](../implementation-plan.md) and the sole dependency [M5-1](../implementation-plan.md#m5-1---real-linear-gateway-and-runtime-wiring) is `Done`.
2. Updated active-plan status row from `Todo` to `In progress`.
3. Confirmed the four overlapping types in [linear-client src/linear_client/types.py](../../../linear-client/src/linear_client/types.py): `IssueId` (line 18), `IssueKey` (line 21), `CommentId` (line 36), `AttachmentId` (line 42) — all `NewType("...", str)`, semantically identical to the context-sync versions.
4. Identified bridge import workaround in [src/context_sync/_real_gateway.py](../../src/context_sync/_real_gateway.py): `from linear_client.types import IssueId as UpstreamIssueId` (lines 312, 467) and `IssueKey as UpstreamIssueKey` (line 313) — these exist because Pyright treats the two NewType definitions as distinct types.
5. Phase A adopted a `TYPE_CHECKING`-only import strategy for the shared ID aliases and removed the old bridge imports from [src/context_sync/_real_gateway.py](../../src/context_sync/_real_gateway.py), which resolved the immediate Pyright friction.
6. Phase B review in [docs/execution/M5-4-review.md](M5-4-review.md) identified four remaining contract gaps:
   - [M5-4-R1](M5-4-review.md#findings): runtime alias identity still diverged from `linear-client`
   - [M5-4-R2](M5-4-review.md#findings): `Timestamp` still used a local runtime alias instead of upstream `IsoTimestamp`
   - [M5-4-R3](M5-4-review.md#findings): `AttachmentData.url` still widened to bare `str`
   - [M5-4-R4](M5-4-review.md#findings): `RelationData.relation_type` still widened to bare `str`
7. Phase C updated [src/context_sync/_types.py](../../src/context_sync/_types.py) to prefer runtime imports from `linear_client.types` whenever the dependency is available. `IssueId`, `IssueKey`, `CommentId`, `AttachmentId`, `AssetUrl`, and `IssueLinkType` now share upstream identity directly, and `Timestamp` preserves its local public name while aliasing upstream `IsoTimestamp`. Local `NewType` fallbacks remain only for environments where `linear-client` is not importable. `WriterId`, `WorkspaceId`, and `WorkspaceSlug` remain context-sync-only aliases.
8. Phase C updated [src/context_sync/_gateway.py](../../src/context_sync/_gateway.py) so the public boundary dataclasses now advertise the stronger semantic types selected by the audit: `AttachmentData.url: AssetUrl` and `RelationData.relation_type: IssueLinkType`.
9. Phase C updated [src/context_sync/_real_gateway.py](../../src/context_sync/_real_gateway.py) so attachment conversion preserves `AssetUrl`, relation normalization preserves `IssueLinkType`, and the raw-GraphQL refresh relation helpers explicitly wrap response strings into `IssueLinkType` before normalizing them.
10. Added regression coverage in [tests/test_package.py](../../tests/test_package.py) and [tests/test_gateway.py](../../tests/test_gateway.py):
   - package-surface identity checks assert `context_sync.IssueId`, `IssueKey`, `CommentId`, `AttachmentId`, and `Timestamp` are the same alias objects as the installed upstream types
   - dataclass type-surface checks assert `AttachmentData.url` resolves to upstream `AssetUrl` and `RelationData.relation_type` resolves to upstream `IssueLinkType`
11. Re-ran the repository's canonical quality gate from an activated virtualenv with `bash` [scripts/validate.sh](../../scripts/validate.sh). All declared gates passed:
   - Ruff lint
   - Ruff format check
   - Pyright
   - Pytest with coverage
12. Verified implementation changes against [docs/policies/common/coding-guidelines.md](../policies/common/coding-guidelines.md) and [docs/policies/common/python/coding-guidelines.md](../policies/common/python/coding-guidelines.md): no new `Any` in signatures, docstrings/type surfaces updated with the new contract, no banned terminology introduced, and all touched Python modules remain comfortably under the file-size limit.
13. After the accepted `Fix now` responses landed and validation passed, updated [docs/execution/M5-4-review.md](M5-4-review.md) to mark [M5-4-R1](M5-4-review.md#findings), [M5-4-R2](M5-4-review.md#findings), [M5-4-R3](M5-4-review.md#findings), and [M5-4-R4](M5-4-review.md#findings) `Done`, then updated the active-plan row for [M5-4](../implementation-plan.md#m5-4---adopt-linear-client-newtypes) to `Done`.

## Completion Summary

**Produced:**
- [src/context_sync/_types.py](../../src/context_sync/_types.py): shared boundary aliases now prefer runtime imports from `linear_client.types`, including `Timestamp` as the local public name over upstream `IsoTimestamp`. Fallback `NewType` definitions remain only for environments where `linear-client` is absent.
- [src/context_sync/_gateway.py](../../src/context_sync/_gateway.py): `AttachmentData.url` now uses `AssetUrl`, and `RelationData.relation_type` now uses `IssueLinkType`.
- [src/context_sync/_real_gateway.py](../../src/context_sync/_real_gateway.py): all legacy bridge imports remain removed, attachment conversion preserves `AssetUrl`, and both domain-layer and raw-GraphQL relation paths preserve `IssueLinkType`.
- [tests/test_package.py](../../tests/test_package.py) and [tests/test_gateway.py](../../tests/test_gateway.py): new regression coverage locks the package-surface alias identity and the dataclass type surface to the installed upstream semantic vocabulary.

**Validations performed:**
- `bash` [scripts/validate.sh](../../scripts/validate.sh) (from an activated `.venv`) — all gates passed.
- Ruff lint: all checks passed.
- Ruff format check: 43 files already formatted.
- Pyright: 0 errors, 0 warnings, 0 informations.
- Pytest with coverage: 580 passed in 10.66s.
- Coding-guidelines compliance: no new `Any`, no magic numbers introduced, docstrings/type hints updated to match the shipped public contract, and touched files remain under 1000 LOC.

**No changelog entry required:** repository is pre-`1.0.0` (`0.1.0.dev0`).

**No temporary scaffolding introduced.**

**Follow-up items:** None.
