"""
Canonical package version for context-sync.

All version-reporting surfaces (pyproject.toml, CLI, docs) derive from this
single source so that version literals are never duplicated.
"""

__version__ = "0.1.0.dev0"
